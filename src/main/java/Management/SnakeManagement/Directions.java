package Management.SnakeManagement;

/**
 * an enum for the game Better Snake which sets the directions possible for the snake to move to
 */
public enum Directions {
    LEFT,
    RIGHT,
    UP,
    DOWN;
}
